<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Communityshare Web" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Communityshare Web" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <link href="/assets/stylesheets/carousel.css" rel="stylesheet">
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
       <%= yield %>
    <main role="main">
       <%= render 'layouts/messages' %>
    </main>
  </body>
</html>
